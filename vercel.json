{
    "installCommand": "pnpm install --no-frozen-lockfile",
    "buildCommand": "cd artifacts/portfolio && npx vite build --config vite.config.ts",
    "outputDirectory": "artifacts/portfolio/dist",
    "framework": null,
    "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
  }
  